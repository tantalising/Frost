name: flutter_signal
description: "A package providing signal and slot mechanism."
version: 1.0.0
homepage: "not_yet_created_todo_fill_this"

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  test: ^1.24.9
